import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Placeholder test - requires Isar setup for full testing
    expect(true, isTrue);
  });
}